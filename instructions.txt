1. Use `cd` in order to enter the directory to which spectacleOS was downloaded.
2. Run `./setup` to run spectacleOS.
	2a. If it says something like "permission denied", run `chmod 755 setup`. Then try again.
3. Once in the spectacleOS shell, type `help`.